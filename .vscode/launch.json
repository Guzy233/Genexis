{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Wails: Debug Full Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}", 
            "buildFlags": "-tags dev",
            "preLaunchTask": "wails:pre-debug"
        }
    ]
}